export const isRotate = ({ key }: KeyboardEvent): boolean => key === '[' || key === ']'
export const rotate = () => undefined
export const keyCombination = [{ key: '[' }, { key: ']' }]
export const keys = [['['], [']']]
