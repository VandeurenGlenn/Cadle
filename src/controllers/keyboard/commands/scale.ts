export const isScale = ({ key }: KeyboardEvent): boolean => key === '/' || key === '*'
export const scale = () => undefined
export const keyCombination = [{ key: '/' }, { key: '*' }]
export const keys = [['/'], ['*']]